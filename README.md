# Jova
JOVA is a human-friendly, machine-predictable data format that extends JSON with native single-line, block, and inline comments. Built for configuration, manifests, tooling, and structured data where developers need documentation beside the values it explains—without turning configuration into a programming language.
