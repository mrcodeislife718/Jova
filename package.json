{
  "name": "jova",
  "version": "0.4.0",
  "description": "JOVA reference implementation with native comments, lossless syntax, transactional editing, undo/redo, change ranges, and persistent syntax identity reconciliation.",
  "type": "module",
  "bin": { "jova": "./src/cli.js" },
  "exports": "./src/index.js",
  "scripts": { "test": "node --test", "check": "npm test" },
  "engines": { "node": ">=20" },
  "license": "MIT"
}
